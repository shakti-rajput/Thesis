
// #pragma once

// #include <iostream>
// #include <list>
// #include <unordered_map>
// #include <vector>

// using namespace std;

// class Sobit {
// private:
//   long long int subject;
//   long long int object;

//   list<long long int> ss;
//   list<long long int> so;
//   list<long long int> os;
//   list<long long int> oo;

// public:
//   Sobit();
//   Sobit(long long int subject, long long int object, list<long long int> ss,
//         list<long long int> so, list<long long int> os, list<long long int> oo);

//   void setData(long long int subject, long long int object,
//                list<long long int> ss, list<long long int> so,
//                list<long long int> os, list<long long int> oo);

//   vector<list<long long int>> getData() const;
// };

// void maptoSobit(list<pair<int, int>> table, list<Sobit> &sobitTable);